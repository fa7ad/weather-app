import { createAction } from 'redux-starter-kit';

export const setWeather = createAction('SET_WEATHER');
export const setToday = createAction('SET_TODAY');
export const setTomorrow = createAction('SET_TOMORROW');
export const setAfter = createAction('SET_AFTER');
